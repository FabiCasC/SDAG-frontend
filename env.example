# Copia este archivo como env.json en la raíz del proyecto.
# Formato: una variable por línea, KEY=valor (aunque el archivo se llame .json).
# No subas env.json a Git si contiene claves reales.

SUPABASE_URL=
SUPABASE_ANON_KEY=
GOOGLE_MAPS_API_KEY=
CULQI_PUBLIC_KEY=
